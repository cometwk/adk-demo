本项目是一个demo原型验证项目，有很多不同的版本, 比如 v3, v4, 还可能有更多

比如

```
./docs
├── think_v3.md     # v3 设计
├── think_v4.md     # v4 设计

./src
├── lib         # 公共
├── v3          # v3 代码
├── v4          # v4 代码
└── v5          # v5 代码 (决策支持 Agent)
```
