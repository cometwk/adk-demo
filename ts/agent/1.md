好的，请根据前面2个回答中的内容，

按照 

1. 九、推荐架构：G = {E,R,T,C} + Compute Layer
2. 十二、最合理的执行链

采用全部的建议

1. 建议 1：移除 graph_query.aggregate : 
2. 建议 2：增加 compute_query
3. 建议 3：GraphStore 不应承担分析职责
4. 建议 4：引入 Query Planner

模仿 
我提供的设计文档《Graph Query 设计文档》
的风格，重写 设计文档

---

目标：重构 v6 的 Graph Query Runtime 在新的目录 src/v8/engine 之下, 重构为 Semantic Reasoning Runtime。

参考 v6 的内容
- src/v6 目录下的代码
- 老版本的设计文档  src/v8/docs/v1.md , 以及允许例子 src/v8/docs/1.json

新版本的要求
- 按照 src/v8/docs/src/v8/docs/v2.1.md 的要求重构
- 本次重构，只处理 Agent + Graph Query Runtime 相关的部分，其他比如 rule 之类的，先不动

考虑到本次重构变化巨大，按照 《新架构总览》



```text
                    User Intent
                          │
                          ▼
                ┌──────────────────┐
                │    LLM Agent     │
                │ (Semantic Reason)│
                └────────┬─────────┘
                         │
                         │ tool intent
                         ▼
                ┌──────────────────┐
                │ Runtime          │
                │ Orchestrator     │
                │（执行编排 / 策略）  │
                └────────┬─────────┘
                         │
              ┌──────────┼────────────┐
              ▼          ▼            ▼
        graph_query  compute_query  vector_query
         （Traversal）  （OLAP）       （Semantic Search）
              │          │            │
              ▼          ▼            ▼
         GraphStore  ComputeStore   VectorStore
              │          │            │
              ▼          ▼            ▼
              └──────────┼────────────┘
                         ▼            
                      FactStore
                  （事实 / 中间状态）
```

我的大概计划：

第一阶段：

1. 先设计 GraphStore    ComputeStore   VectorStore 三者, 以及 Runtime Orchestrator  的接口和文档, FactStore 保留 v6 不变，或者简化
2. tools 接口文件和实现
3. LLM Agent 运行模型 基本上跟 v6 类似

第二阶段：

分别讨论和落地 GraphStore    ComputeStore   VectorStore 的具体实现


先从第一阶段的设计文档开始，希望输出第一阶段的详细设计文档


---

目标：重构 v6 的 ontology 在新的目录 src/v8/ontology 之下

- V6 的代码目录：src/v6 
- V8 的代码目录：src/v6 


按照架构设计:

V6：

```text
G = {E, R, T, C}
```

V8：

```text
G = {
  E,   // Entities
  R,   // Relations
  T,   // Types
  C,   // Constraints / Rules
  Q    // Query & Compute Layer
}
```

在 V8 中
- Q = src/v8/engine 已经完成

接下来处理 `G = {E, R, T, C}` 除了 C 之前的部分： 本体 相关的内容

先阅读
- docs/design/1-graph-layers.md
- src/v8/docs/engine/graph-query-draft.md
- src/v8/docs/engine/phase1-design.md

主要内容包含：

- BaseNode
     - src/v6/runtime/graph.ts
- 本体 Schema RelationSchema 相关
     - src/v6/ontology/schema.ts
     - src/v6/ontology/validate-bindings.ts
     - src/v6/ontology/relation-binding.ts
- 本体 注解，构造 等
     - src/v6/runtime/ontology-builder.ts
     - src/v6/runtime/registry.ts
     - src/v6/runtime/decorator.ts
- 本体 tools
     - src/v6/agent/tools/ontology.ts

不包含的内容:

- GraphStore 相关的内容, 已经处理
- rule 规则相关的内容，待后续处理



模仿 
src/v8/docs/engine/phase1-design.md
的风格，先编写设计文档到 src/v8/docs/ontology 目录