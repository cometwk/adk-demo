好的，请根据前面2个回答中的内容，

按照 

1. 九、推荐架构：G = {E,R,T,C} + Compute Layer
2. 十二、最合理的执行链

采用全部的建议

1. 建议 1：移除 graph_query.aggregate : 
2. 建议 2：增加 compute_query
3. 建议 3：GraphStore 不应承担分析职责
4. 建议 4：引入 Query Planner

模仿 
我提供的设计文档《Graph Query 设计文档》
的风格，重写 设计文档

---

目标：重构 v6 的 Graph Query Runtime 在新的目录 src/v8/engine 之下, 重构为 Semantic Reasoning Runtime。

参考 v6 的内容
- src/v6 目录下的代码
- 老版本的设计文档  src/v8/docs/v1.md , 以及允许例子 src/v8/docs/1.json

新版本的要求
- 按照 src/v8/docs/src/v8/docs/v2.1.md 的要求重构
- 本次重构，只处理 Agent + Graph Query Runtime 相关的部分，其他比如 rule 之类的，先不动

考虑到本次重构变化巨大，按照 《新架构总览》



```text
                    User Intent
                          │
                          ▼
                ┌──────────────────┐
                │    LLM Agent     │
                │ (Semantic Reason)│
                └────────┬─────────┘
                         │
                         │ tool intent
                         ▼
                ┌──────────────────┐
                │ Runtime          │
                │ Orchestrator     │
                │（执行编排 / 策略）  │
                └────────┬─────────┘
                         │
              ┌──────────┼────────────┐
              ▼          ▼            ▼
        graph_query  compute_query  vector_query
         （Traversal）  （OLAP）       （Semantic Search）
              │          │            │
              ▼          ▼            ▼
         GraphStore  ComputeStore   VectorStore
              │          │            │
              ▼          ▼            ▼
              └──────────┼────────────┘
                         ▼            
                      FactStore
                  （事实 / 中间状态）
```

我的大概计划：

第一阶段：

1. 先设计 GraphStore    ComputeStore   VectorStore 三者, 以及 Runtime Orchestrator  的接口和文档, FactStore 保留 v6 不变，或者简化
2. tools 接口文件和实现
3. LLM Agent 运行模型 基本上跟 v6 类似

第二阶段：

分别讨论和落地 GraphStore    ComputeStore   VectorStore 的具体实现


先从第一阶段的设计文档开始，希望输出第一阶段的详细设计文档


---

目标：重构 v6 的 ontology 在新的目录 src/v8/ontology 之下

- V6 的代码目录：src/v6 
- V8 的代码目录：src/v6 


按照架构设计:

V6：

```text
G = {E, R, T, C}
```

V8：

```text
G = {
  E,   // Entities
  R,   // Relations
  T,   // Types
  C,   // Constraints / Rules
  Q    // Query & Compute Layer
}
```

在 V8 中
- Q = src/v8/engine 已经完成

接下来处理 `G = {E, R, T, C}` 除了 C 之前的部分： 本体 相关的内容

先阅读
- docs/design/1-graph-layers.md
- src/v8/docs/engine/graph-query-draft.md
- src/v8/docs/engine/phase1-design.md

主要内容包含：

- BaseNode
     - src/v6/runtime/graph.ts
- 本体 Schema RelationSchema 相关
     - src/v6/ontology/schema.ts
     - src/v6/ontology/validate-bindings.ts
     - src/v6/ontology/relation-binding.ts
- 本体 注解，构造 等
     - src/v6/runtime/ontology-builder.ts
     - src/v6/runtime/registry.ts
     - src/v6/runtime/decorator.ts
- 本体 tools
     - src/v6/agent/tools/ontology.ts

不包含的内容:

- GraphStore 相关的内容, 已经处理
- rule 规则相关的内容，待后续处理



模仿 
src/v8/docs/engine/phase1-design.md
的风格，先编写设计文档到 src/v8/docs/ontology 目录


---

目标：重构 v6 的 远程 Rest Query 的实现 src/v6/provider/rest 在新的目录 src/v8/provider/rest-query 之下


模仿 
src/v8/docs/engine/phase1-design.md
的风格，先编写设计文档到 src/v8/docs/rest-query 目录


---


目标：重构 v6 的 rule 模型, 到新的 v8 目录 src/v8/rule 之下

v6 相关的代码:

- src/v6/agent/tools/rules.ts
- src/v6/ontology/ruleDag.ts
- src/v6/ontology/rules.ts

v8 设计草稿:

- src/v8/docs/rule/v8_rule_design_draft.md


根据设计草稿，结合 v6 的代码，以及 v8 其他的模块要求，

模仿
src/v8/docs/rule/v8_rule_design_draft.md
的风格，重构编写设计文档到 src/v8/docs/rule/design.md 


---

目标：重构 v6 的 pipeline 模块, 到新的 v8 目录 src/v8/pipeline 之下


参考 v6 的材料:

- docs/design/3-pipeline-design.md

及其代码  

- src/v6/agent
- src/v6/frontend

要求结合现有的V8模块

- src/v8/docs/engine       # Semantic Reasoning Runtime
- src/v8/docs/ontology     # 本体层
- src/v8/docs/rule         # 规则层

及其对应的代码

- src/v8/engine
- src/v8/ontology
- src/v8/rule


核心概念：

类似将业务抽象为 本体 之后，类似 src/v6/agent/prompt.ts 的提示词，
在基于 业务本地层 的基础上，可以执行不同的任务.
任务定义在，类似 src/v6/ontology/decision.ts 中的定义的任务.

比如目前已经存在的

- Diagnostic (后向归因)
- Predictive（前向推断

在 v8 中，还要求扩展，到自定义任务类型，基于通用的 本体层 提示词之外，自定义提示词
- 比如 src/v8/engine/agent/executor.ts 中的 ReasoningTask
- 注意： src/v8/engine/agent/executor.ts 未来将被移动到 pipeline 中，作为新的任务类型


Agent 的大致推理过程如下：

- frontend: 识别用户意图，初始化任务类型，以及入口实体
- executor: 基于 ontology 通过跟 engine 提供的数据，  执行 LLM Agent ReasoningTask, 收集数据
- critic: 根据不同的任务，对数据做处理

另外，还要类似 src/v6/tests/1-graph/helper.ts 中的模式：
- 类似 newAgentContext 初始创建特定任务的agent，后续能返回基于某个任务对话
- 类似 streamPredictiveAgent / syncPredictiveAgent 提供 stream and sync 2种API

要求： 不同的任务，要分不同的子目录处理，便于扩展

模仿
src/v8/docs/engine/phase1-design.md
的风格，编写 pipeline 模块的设计文档到 src/v8/docs/pipeline/draft.md 

---

结合 src/v8/engine/query 以及 src/v8/engine/query 中的文档和代码，
编写 GraphStore 和 ComputeStore 的使用手册，
写入 src/v8/guide/engine 目录中
