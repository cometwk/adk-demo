类似 components/chat/app-sidebar.tsx 中的  
                    <span className="font-medium">Import JSON</span>
菜单项，

创建一个新的菜单项 "Graph Agent"， 先弹出一个对话框，输入 一段文本， 该文本作为
用户的第一条 chat 输入，
1. 跟 "Import JSON" 功能一样, 输入的文本，来找文件，or textarea
2. 不一样的是，文本是作为 user message 进入 chat messages, 而不是 json messages 导入

